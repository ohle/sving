package de.eudaemon.sving.core;

import java.awt.*;
import java.util.stream.Stream;

public class Hinter {
    public Stream<Hint> findHints(Container target_) {
        return Stream.empty();
    }
}
